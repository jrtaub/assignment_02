I struggled a lot with this assignment, especially with understanding how the different parts of the ETL pipeline worked together. One thing I learned was how data coercion is used to turn messy values, like prices stored as strings or missing quantities, into usable numbers without crashing the program. I also better understand how an accumulator can be used to group data, such as using summarize_by_item to combine sales by product or summarize_by_day to combine sales by date. I still struggled with understanding how the Python package, __init__.py, relative imports, and the public API connected the different modules and reports. I also found it difficult to interpret integration test failures because sometimes the problem was not in the function that appeared to fail, but somewhere earlier in the pipeline. Going forward, I want to practice tracing the input and output of each function, writing out what type of data each function expects before I code, and using the debugger to inspect variable values when a test fails instead of immediately changing my code. I also want more practice comparing unit tests and integration tests so I can better identify whether an error is inside one function or caused by how multiple parts of the program work together.